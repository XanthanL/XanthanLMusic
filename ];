
## 3. 核心视觉与交互设计
1.  **黑胶唱片动效 (Vinyl Component)**:
    *   当音乐播放时，专辑封面呈黑胶唱片样式旋转（使用 `Framer Motion` 控制 `animate={{ rotate: 360 }}`）。
    *   暂停时，唱片指针（Stylus）优雅抬起，旋转动画平滑减速停止。
2.  **毛玻璃背景 (Glassmorphism)**:
    *   背景采用当前播放歌曲专辑封面的高斯模糊版本，营造极具艺术感的沉浸式氛围。
3.  **歌词同步滚动**:
    *   解析 `.lrc` 格式歌词，根据当前播放的 `currentTime` 自动高亮并滚动到对应行。

## 4. 后续开发步骤
1.  **第一步**：在 `apps/web/config/music.ts` 中，将您放入 `public/music/` 的实际文件名填入播放列表配置中。
2.  **第二步**：编写 `Player.tsx` 播放器组件，使用 HTML5 `Audio` 对象加载 `/music/xxx.mp3`。
3.  **第三步**：加入 Framer Motion 动画，让界面随着音乐律动起来。
